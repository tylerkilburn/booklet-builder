import { IBooklet } from "../types"
import { pagePrayerLog } from "./pagePrayerLog"

export const bookletPrayerLog: IBooklet = {
  pages: [
    pagePrayerLog,
    pagePrayerLog,
    pagePrayerLog,
    pagePrayerLog,
    pagePrayerLog,
    pagePrayerLog,
    pagePrayerLog,
    pagePrayerLog,
  ],
}
